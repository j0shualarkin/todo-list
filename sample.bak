#lang typed/racket 


(define-syntax (TODO* stx)
  (syntax-case stx ()
    [(_ msg)
     ;; Attach a notice that it is a TODO to be filled out
     (syntax-property (syntax/loc stx
                        (error msg))
                      'todo
                      (vector "--------------------\n"
                              (syntax->datum #'msg)))]))


(let ([x 5])
  (let ([y 6])
    (let ([z x])
      (+ x y (ann (TODO* "oops") Number)))))



